/** @format */

//hoisted
console.log(x);
console.log(y);
var x = 106; // js internally declare var in top of page but assign value while we reach the line of assign its 5 in our case
let y = 106; // let is also hoisted, but
