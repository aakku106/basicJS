/** @format */

let names = ["ccn", "aakku", "ram", "hari", "sita"];
cat = names.map((value, index) => {
  //   console.log(value, " in index ", index);
  return value + "wee";
});

console.log(names);
console.log(cat);
